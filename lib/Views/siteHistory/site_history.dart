import 'package:flutter/material.dart';

class SiteHistory extends StatelessWidget {
  const SiteHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}